class FourCal:
  def setdata(self, first, second):  # 사칙연산을 위해 두 숫자를 입력 받아
    self.first=first
    self.second=second
    
   def add(self):
    result=self.first + self.second   # 더하기 기능
    return result
